# Style: local additions

Yours. Seeded empty and never overwritten, so anything here survives a kit upgrade.

`STYLE.md` tracks the kit and gets its fixes. Put this knowledge base's own additions and
exceptions here instead of editing that file: house terms to prefer or avoid, a client's naming
conventions, anything specific to this subject. Read after `STYLE.md`, so it wins on a conflict.

# junk/

Files `scripts/sweep` set aside here because their name matched `.ingestignore` (system cruft,
temp and lock files). They are not sources and are never ingested. Review and delete them. This
directory is gitignored; only this README is tracked.
